using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ValenceScript : MonoBehaviour
{
    public int ionCharge;  //positive for cations, negative for anions
    public bool Bonded;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
