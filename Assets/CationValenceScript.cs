using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CationValenceScript : MonoBehaviour
{
    public int Valence;
    //public int CationID;  //used for the answer choices (superseded by SaltID in TMPDrowdownSaltSelectorScript)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
